# Code generated by sqlc. DO NOT EDIT.
# versions:
#   sqlc v1.30.0
# source: guest.sql
from typing import Optional
import uuid

import sqlalchemy

from libs.infrastructure.db.gen import models


CREATE_GUEST = """-- name: create_guest \\:one
INSERT INTO guests (name, contact)
VALUES (:p1, :p2)
RETURNING id
"""


class Querier:
    def __init__(self, conn: sqlalchemy.engine.Connection):
        self._conn = conn

    def create_guest(self, *, name: str, contact: str) -> Optional[uuid.UUID]:
        row = self._conn.execute(sqlalchemy.text(CREATE_GUEST), {"p1": name, "p2": contact}).first()
        if row is None:
            return None
        return row[0]
