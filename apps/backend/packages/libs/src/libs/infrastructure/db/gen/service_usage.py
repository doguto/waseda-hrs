# Code generated by sqlc. DO NOT EDIT.
# versions:
#   sqlc v1.30.0
# source: service_usage.sql
import pydantic
from typing import Iterator
import uuid

import sqlalchemy

from libs.infrastructure.db.gen import models


LIST_SERVICE_USAGES = """-- name: list_service_usages \\:many
SELECT service_name, fee
FROM service_usages
WHERE reservation_id = :p1
ORDER BY service_name
"""


class ListServiceUsagesRow(pydantic.BaseModel):
    service_name: str
    fee: int


class Querier:
    def __init__(self, conn: sqlalchemy.engine.Connection):
        self._conn = conn

    def list_service_usages(self, *, reservation_id: uuid.UUID) -> Iterator[ListServiceUsagesRow]:
        result = self._conn.execute(sqlalchemy.text(LIST_SERVICE_USAGES), {"p1": reservation_id})
        for row in result:
            yield ListServiceUsagesRow(
                service_name=row[0],
                fee=row[1],
            )
