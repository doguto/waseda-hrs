# Code generated by sqlc. DO NOT EDIT.
# versions:
#   sqlc v1.30.0
# source: reservation.sql
import datetime
import pydantic
from typing import Optional
import uuid

import sqlalchemy

from libs.infrastructure.db.gen import models


GET_RESERVATION_BY_ID = """-- name: get_reservation_by_id \\:one
SELECT
    r.id,
    r.check_in_date,
    r.check_out_date,
    r.status,
    g.name AS guest_name,
    g.contact AS guest_contact,
    rm.room_number,
    rm.room_type
FROM reservations AS r
INNER JOIN guests AS g ON g.id = r.guest_id
INNER JOIN rooms AS rm ON rm.room_number = r.room_number
WHERE r.id = :p1
"""


class GetReservationByIdRow(pydantic.BaseModel):
    id: uuid.UUID
    check_in_date: datetime.date
    check_out_date: datetime.date
    status: models.ReservationStatus
    guest_name: str
    guest_contact: str
    room_number: str
    room_type: str


class Querier:
    def __init__(self, conn: sqlalchemy.engine.Connection):
        self._conn = conn

    def get_reservation_by_id(self, *, id: uuid.UUID) -> Optional[GetReservationByIdRow]:
        row = self._conn.execute(sqlalchemy.text(GET_RESERVATION_BY_ID), {"p1": id}).first()
        if row is None:
            return None
        return GetReservationByIdRow(
            id=row[0],
            check_in_date=row[1],
            check_out_date=row[2],
            status=row[3],
            guest_name=row[4],
            guest_contact=row[5],
            room_number=row[6],
            room_type=row[7],
        )
