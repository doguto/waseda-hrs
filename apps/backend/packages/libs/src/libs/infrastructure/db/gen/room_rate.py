# Code generated by sqlc. DO NOT EDIT.
# versions:
#   sqlc v1.30.0
# source: room_rate.sql
from typing import Optional

import sqlalchemy

from libs.infrastructure.db.gen import models


GET_ROOM_RATE = """-- name: get_room_rate \\:one
SELECT room_type, price_per_night
FROM room_rates
WHERE room_type = :p1
"""


class Querier:
    def __init__(self, conn: sqlalchemy.engine.Connection):
        self._conn = conn

    def get_room_rate(self, *, room_type: str) -> Optional[models.RoomRate]:
        row = self._conn.execute(sqlalchemy.text(GET_ROOM_RATE), {"p1": room_type}).first()
        if row is None:
            return None
        return models.RoomRate(
            room_type=row[0],
            price_per_night=row[1],
        )
